#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        string st;
        cin>>st;
        vector<int> zeros;
        vector<int> ones;
        for(int i=0;i<n;i++){
            if(st[i]=='0') zeros.push_back(i);
            else ones.push_back(i);
        }
        bool flag=true;
        while(flag){
            
        }
    }
}